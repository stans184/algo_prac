# 정사각형의 한 변의 길이 n을 입력받은 후 다음과 같은 문자로 된 정사각형 형태로 출력하는 프로그램을 작성하시오.

# < 처리조건 >
# 문자의 진행 순서는 맨 오른쪽 아래에서 위쪽으로 'A'부터 차례대로 채워나가는 방법으로 아래 표와 같이 왼쪽 위까지 채워 넣는다. 
# 'Z' 다음에는 다시 'A'부터 반복된다.

# 정사각형 한 변의 길이 n(n의 범위는 1이상 100 이하의 정수)을 입력받는다.
# 위의 형식과 같이 한변의 길이가 n인 문자 사각형을 출력한다. 문자 사이는 공백으로 구분하여 출력한다.

# 정사각형 한 변의 길이 n 입력 받기
n = int(input("정사각형 한 변의 길이를 입력하세요: "))

# 문자 정사각형 출력
for i in range(n):
    for j in range(n):
        # ASCII 코드를 사용하여 문자 계산 (행 기준으로 아래에서 위로, 왼쪽에서 오른쪽으로 시작 문자 변경)
        char = chr((n - 1 - i + j * n) % 26 + ord('A'))
        print(char, end=" ")
    print()  # 줄바꿈
    
# 코드 수정필요
# input = 8
# L D V N F X P H 
# K C U M E W O G 
# J B T L D V N F 
# I A S K C U M E 
# H Z R J B T L D 
# G Y Q I A S K C 
# F X P H Z R J B 
# E W O G Y Q I A 